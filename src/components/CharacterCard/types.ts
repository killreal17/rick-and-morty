export type CardProps = {
  characterId: number,
}

export type TextProps = {
  color?: string, 
  size?: 'l' | 'm' | 's',
}

export type LoadingPlaceProps = {
  size?: 'l' | 'm' | 's',
}

export type ImageProps = {
  isAnimated: boolean,
}
